name: bock_foods
description: A starter Flutter app for Bock Foods (Food + Instamart) - Green theme.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.2.1 # Or the latest version
  shared_preferences: ^2.2.3 # Or the latest version

  provider: ^6.0.5
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
 # assets:
 #  - assets/images/
